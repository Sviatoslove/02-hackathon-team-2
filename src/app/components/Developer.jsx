import React from 'react'
import PropTypes from 'prop-types'

const Developer = ({ developer }) => {
  return <div>Developrer</div>
}

Developer.propTypes = {
  developer: PropTypes.object
}

export default Developer
